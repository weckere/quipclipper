# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# pytest / tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# quipclipper output (don't commit generated clips)
*.mp3
*.wav
*.opus
*.mp4
*.gif

# quipclipper-web runtime dirs (default locations when run from a checkout)
/clips/
/state/
web/backend/clips/
web/backend/state/

# Nix
result

# OS
.DS_Store

# Claude Code local settings (machine-specific; may reference local paths/hosts)
.claude/

# Internal dev/handoff notes — kept locally for ongoing work, not for the
# public repo (they reference personal deploy infra).
/HANDOFF.md
/docs/HANDOFF.md
/docs/WEBAPP_PROGRESS.md
/web/docker-compose.omv.yml
